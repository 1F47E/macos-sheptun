import SwiftUI

struct TranscribeDebugView: View {
    var body: some View {
        VStack {
            Text("hello")
                .font(.largeTitle)
                .padding()
            Spacer()
        }
        .frame(minWidth: 400, minHeight: 300)
    }
} 